apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.orderProcessor.name }}
  labels:
    app: {{ .Values.orderProcessor.name }}
spec:
  replicas: {{ .Values.orderProcessor.replicaCount | default .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.orderProcessor.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.orderProcessor.name }}
    spec:
      containers:
        - name: {{ .Values.orderProcessor.name }}
          image: "{{ .Values.orderProcessor.image.repository }}:{{ .Values.orderProcessor.image.tag }}"
          imagePullPolicy: {{ .Values.orderProcessor.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.orderProcessor.service.targetPort }}
              protocol: TCP
          env:
            - name: NOTIFICATION_SERVICE_URL
              value: {{ .Values.orderProcessor.env.notificationServiceUrl | quote }}
            - name: PORT
              value: {{ .Values.orderProcessor.service.targetPort | quote }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.orderProcessor.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.orderProcessor.name }}
  labels:
    app: {{ .Values.orderProcessor.name }}
spec:
  type: {{ .Values.orderProcessor.service.type }}
  ports:
    - port: {{ .Values.orderProcessor.service.port }}
      targetPort: {{ .Values.orderProcessor.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.orderProcessor.name }}
